[project]
name = "snprg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jean Snyman", email = "git@jsnyman.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
snprg = "snprg:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.59",
]
